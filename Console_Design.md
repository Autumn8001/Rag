# Enterprise RAG 企业知识证据工作台 (RAG Evidence Workspace) 交互与视觉设计方案

本设计方案旨在将 Enterprise RAG 从普通单调的“ChatGPT 聊天克隆”重塑为专业的 **企业知识证据工作台 (RAG Evidence Workspace)**。整个界面旨在让第一层用户 (HR) 快速理解其作为安全企业知识库的价值，让第二层用户 (技术面试官) 能够直观探索混合检索、Critic Agent、多租户隔离、安全指纹审计以及 RAG 性能 Trace 链路等硬核后端工程能力。

---

## 🎨 视觉系统规范 (Visual System)

* **色彩系统 (Color Scheme)**
  * **左侧导航栏 (深色区)**：采用冷炭黑 (`#16181A`)，与核心工作区拉开对比，烘托企业级管理后台的沉稳感。
  * **主工作区 (浅色区)**：使用温润燕麦偏白色 (`#FBFBFA`)，文字为深黑 (`#1A1A1A`)，确保长时间问答审计时的舒适度。
  * **右侧 Evidence Dock (中间色区)**：采用淡雅沙质灰色 (`#F5F4EE`)，边框采用泥土灰细线 (`#E6E2D8`)。
  * **状态指示色 (Functional Colors)**：
    * `Teal` (松石绿, `#0D9488`)：表示**安全可信**，如 Critic Agent 幻觉校验通过、租户沙箱隔离中、指纹校验通过。
    * `Blue` (群青蓝, `#2563EB`)：表示**检索与算法链路**，如多路召回、RRF 混合检索、Flashrank Rerank 重新排序。
    * `Amber` (琥珀黄, `#D97706`)：表示**审计预警与风险**，如 Critic 检测到文本可能存在幻觉、敏感词过滤、指纹重复跳过。
* **几何与圆角**
  * 页面中所有交互卡片、输入框、按钮和徽章统一采用精致的 **8px 或 4px 极窄直角圆角** (对应 Tailwind `rounded-lg` / `rounded`)，消除过度圆滑的 C 端消费级玩具感，塑造科技冷峻的硬朗几何结构。
* **排版与间距**
  * 使用系统无衬线字体 (`Inter` 与 `system-ui` 混合) 作为主文字；在 Rank 指数、RRF 分数、哈希指纹和时延计算等数据处，使用 `JetBrains Mono` 或 `Fira Code` 等宽字体。
  * 采用极细的 `1px` 实线边框，收紧 padding，提高界面数据密度，提供报表级的数据视感。
  * **彻底避免**：紫色渐变、玻璃态 (Glassmorphism)、装饰性光斑以及任何表情符号 (Emoji)。

---

## 🏗️ 页面空间布局架构

主控制台采用 **三栏三段式固定布局**，支持双侧边栏的折叠隐藏，最大化利用屏幕空间。

```
+-----------------------------------------------------------------------------------------------+
|  LOGO | [MODE: Hybrid + Critic]          Total: 840ms (Rtv:120ms|LLM:640ms)       | [Tenant_visitor]   |  <- 顶部状态栏
+------------------------------------+----------------------------------+-----------------------+
|                                    |                                  |                       |
|  * 租户空间标识 (Teal)             |    消息气泡流                     |  Tab: 证据链召回舱     |
|                                    |    - USER 提问                   |                       |
|  * 知识库文档列表与指纹            |    - AI 证据链分析答案            |  * 检索合并路线图      |
|    - MD5哈希                       |      - Critic Agent 评估结果     |  * 混合召回 RRF 权重   |
|    - 原始文件名                    |      - 文本内嵌锚点 [1] [2]      |  * 切片物理卡片       |
|    - [Chroma 切片审计] 按钮        |                                  |                       |
|                                    |                                  |-----------------------|
|  * 历史会话过滤                    |                                  |  Tab: 指纹审计日志     |
|    - [Hybrid] 会话 1               |                                  |  * 文件 MD5 防重      |
|    - [Semantic] 会话 2             |                                  |  * 物理删除记录        |
|                                    |                                  |                       |
+------------------------------------+----------------------------------+-----------------------+
|  登出/用户空间项                   |  [输入控制区: RRF权重滑块 | 提问]  |  [RAG Trace 链路抽屉]  |  <- 底部控制区
+------------------------------------+----------------------------------+-----------------------+
```

---

## 🧭 细部模块设计方案

### 1. 顶部状态栏 (Top Navbar)
* **左侧**：展示企业 RAG 图标，以及当前运行模式胶囊 `[Hybrid + Critic Mode]` (Teal)。
* **中间**：展示当前回答的**精准 RAG 时延审计数据**（如：`Latency Audit: Total 840ms | Retrieval: 120ms | Critic: 80ms | LLM: 640ms`），使用等宽字体以深灰显示，彰显后端性能观测能力。
* **右侧**：显示当前多租户沙箱标识 `Tenant Sandbox: tenant_visitor_3a8f`，左侧带有一个常亮松石绿点 `● Isolated`，给 HR 和技术面试官强烈的越权防御和多租户沙箱安全感。

### 2. 左侧导航设计 (Sidebar)
* **租户控制区**：顶部显示当前登录用户（如 `visitor_91ac`），下方为当前沙箱存储空间已用占比。
* **我的专属数据源**：
  * 支持拖拽上传区域收纳进左侧。
  * 紧凑的文件列表：每行包含“文件名”、“Chroma 指纹哈希（截短）”，并提供“物理删除 (永久擦除向量分片)”和“切片审计 (直接查看 Chroma Chunks)”按钮。
* **历史会话栏**：历史会话按“今天”、“昨天”、“更早”时间分组。每条会话前缀有微型灰色字标 `[RRF]` 或 `[Vec]`，体现该次对话所选的检索策略。

### 3. 中间问答区设计 (Workspace)
* **气泡重构**：
  * 每次 AI 的回复上方均伴有一行松石绿底色的 Critic 评估条：`Critic Agent: Approved (Confidence: 98%) | No Hallucination Detected`。
  * 如果 Critic 模块判断回复存在安全溢出或幻觉，则显示琥珀黄警示：`Critic Warning: 存在 15% 无法追溯依据的概率`。
  * 文本流中凡是引用到知识库的地方，都会被解析并插入类似 `[1]` 或 `[2]` 的高亮数字按钮，点击后右侧 Panel 会立刻定位并高亮闪烁该来源 Chunks。
* **参数配置胶囊 (超参调节区)**：
  * 输入框上方提供隐藏式超参调节模块：
    * **RRF 权重调节滑块**：允许用户调节 `Vector Weight (0.7) : BM25 Weight (0.3)`。
    * **改写开关**：提供 `Query Rewriting` 开启/关闭胶囊按钮。
    * 这样能让技术面试官在前端就能立刻模拟检索调优，突出系统的混合召回工程细节。

### 4. 右侧 Evidence Dock 设计
作为控制台的核心证据底座，该面板分为两个 Tab：
* **Tab 1: 证据链召回舱 (Citations)**：
  * 展示一个垂直的时间轴路线图，表现检索数据被召回和合并的过程：
    * **RRF 融合成绩**：每张卡片右上方用蓝色等宽字体标注 `RRF Score: 0.0166`。
    * **多路检索原始 Rank**：卡片内部标注 `Vec Rank: #1` 与 `BM25 Rank: #2`，体现混合检索在后端的排序合并细节。
    * **Flashrank Rerank 状态**：如果启动了重排，展现 `Rerank: #3 -> #1`，标明它如何从第 3 位被 Flashrank 提到第 1 位。
    * **Chunk 物理位置与目录级级**：展示此 Chunk 关联的 H1/H2 目录路径，并附带 Chroma 内部 UUID。
* **Tab 2: 指纹与指纹防重审计 (Security Audit)**：
  * 展现文档指纹 MD5 的防重冲突记录（若上传了同一文档，系统指纹防重拦截自动跳过并在这里生成审计日志）。
  * 记录当前租户空间下 Chunks 的物理擦除历史，向面试官展示“物理删除”真的在后端调用了向量擦除。

### 5. RAG Trace 探针调试抽屉 (Trace Drawer)
* 从屏幕底部或侧边展开的隐藏半高抽屉，提供**链路级观测**（模仿极简版 LangSmith Trace 树）：
  * **节点 1: Input** (用户原始问题)
  * **节点 2: Query Rewrite** (后端经过 Rewrite Agent 改写后的多角度新 Queries 列表)
  * **节点 3: Hybrid Retrieve** (Chroma 向量检索 5 条 + SQLite BM25 检索 5 条，共 10 条原始候选)
  * **节点 4: RRF Merge & Flashrank Rerank** (混合检索去重分数融合与重排，裁选出前 3 条最优子集)
  * **节点 5: Critic Evaluation** (Critic 模块对筛选出的证据链与生成的文本做真伪验证)
  * **节点 6: Streaming Output** (打印答案)
  * 面试官通过点击折叠节点，能清晰看懂这个 Enterprise RAG 在后端的底层技术栈。

### 6. 空状态设计 (Landing Workspace)
当新开对话没有消息时，Workspace 正中展示“**企业知识库引擎控制台概览**”，而非简单的空聊天，它包括：
* **专属隔离沙箱摘要**：显示“已加载 3 个加密知识库，当前 Chroma 向量库物理分片总数：142 Chunks，MD5 文档指纹已验证。”
* **预置高级探索入口**（点击即可自动发送提问）：
  * `[混合检索测试]` ：“测试当前知识库在混合多路召回下的检索响应时延。”
  * `[越权防御审计]` ：“使用未授权身份读取本沙箱隔离空间外的底层文档，检验越权拦截效果。”
  * `[Critic 防幻觉验证]` ：“针对知识库外的不相干事实进行提问，测试 Critic Agent 的幻觉打回机制。”

---

## 💡 为什么该设计能突出项目实力？

1. **摆脱“ChatGPT 克隆”偏见**：
   一般的面试作品集大多只是包了一层 OpenAI SDK，而该设计通过展示 RRF Score, Rerank Rank、Critic 校验通过条以及 RAG Trace 树，直接在视觉和功能上将系统的“**二次开发及深度 RAG 算法调优**”工作完全量化并具象化地铺展在面试官眼前。
2. **多租户与安全审计在视觉上的直接感知**：
   通过常亮松石绿点 `● Isolated`、Chroma 切片审计抽屉、MD5 指纹审计日志等，向 HR 和面试官强调：这并非单用户玩具，而是一个设计完备的、能在企业敏感数据沙箱环境下实装的 B 端 SaaS 产品。
3. **极佳的可玩性与超参调整**：
   直接把“混合检索权重滑块”和“RAG 流程节点 Trace”放在问答界面上。技术面试官无需阅读你冗长的 README 说明文档，仅在控制台首页随意提问并拨动滑块，便能立刻通过时延与引用变化体验到你后端的 RAG 架构设计，留存极强的技术印象。
